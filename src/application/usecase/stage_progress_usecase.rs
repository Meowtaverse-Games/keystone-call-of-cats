// Thin compatibility module re-exporting types that currently live alongside
// stage catalog use case. This preserves existing import paths across the codebase.
pub use crate::application::usecase::stage_catalog_usecase::StageProgressServiceRes;
